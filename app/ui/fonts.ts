import {Lato, Inter, Roboto} from 'next/font/google';

export const roboto = Roboto({
    subsets: ['latin'],
    weight: ['400', '700'],
});

export const lato = Lato({
    subsets: ['latin'],
    weight: ['400', '700'],
});